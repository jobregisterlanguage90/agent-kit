# 实体清单 — {{ENTITY_LABEL}}
# alias: SSH/API 别名，label: 显示名称，group: 分组，tags: 标签

entity_type: {{ENTITY_TYPE}}
entity_label: "{{ENTITY_LABEL}}"
connect_via: {{CONNECT_VIA}}    # ssh | api | local

entities:
  # 添加你的实体
  # - alias: my-entity
  #   label: "我的实体"
  #   group: default
  #   tags: [production]
