# {{AGENT_ROLE}}

## 自动模式

本项目启用自动模式。SessionStart hook 会在新会话启动时注入初始化指令。

### Step 0: 首次运行引导（仅 .env 不存在时触发）

SessionStart hook 检测到 `.env` 不存在时，跳过正常启动，执行引导：

1. **环境检查** — 检查 jq、node 是否安装；运行 `bash setup.sh`（安装 hooks + Skills symlink）
2. **安装 Dashboard 依赖** — `cd web && npm install`
3. **收集配置** — 逐组询问用户，参考 `.env.example`
4. **生成 .env** — 所有值收集完后一次性写入
5. **验证连通性** — 遍历 `entities.yaml` 中的实体，逐个测试连接
6. **引导完成** — 告知用户配置完成，切换到正常启动序列

### 启动序列（收到 SessionStart 上下文后自动执行，不要询问用户）

1. **启动 Dashboard 服务** — `bash web/start-dashboard.sh`，然后 `open http://localhost:{{DASHBOARD_PORT}}`
2. **初始化实体** — 读取 `entities.yaml`，通过 `curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/server/init` 初始化
3. **加载已知状态** — 读取 `memory/*.md` 中的指标，通过 `curl POST /api/server/:alias/status` 更新
4. **设置连接状态** — `curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/claude/status -d '{"status":"connected"}'`
5. **创建团队** —
   a. `TeamCreate "{{PROJECT_NAME}}-team"`
   b. 并行 spawn {{TEAM_SIZE}} 个 Worker（单条消息发 {{TEAM_SIZE}} 个 Task tool 调用），见下方 Worker Prompt 模板
   c. 等待所有 Worker 启动完成
6. **启动后台轮询** — `bash scripts/dashboard-poll.sh`，run_in_background（timeout 600000）
7. **Plugin 巡检** — `curl -sf http://localhost:{{DASHBOARD_PORT}}/api/cron/status` 检查各 plugin daemon 状态
8. **报告就绪** — 一句话告知用户 Dashboard 已就绪，Team 已创建，等待指令

### Team 模式（Lead + Worker 架构）

启动序列创建 Team 后，**Lead 只做轮询和调度，不执行任何 Skill**。所有任务由 Worker 执行。

```
轮询发现消息 → Lead 解析 → SendMessage 分发给 Worker（< 1秒）→ 立即重启轮询
                                    ↓
                        Worker 独立执行（不阻塞 Lead）
                                    ↓
                        Worker 完成 → SendMessage 回报 Lead → Lead 汇总给用户
```

**调度规则**：
- 按消息顺序分配给空闲 Worker
- 同一实体的操作分配给同一 Worker（memory 文件安全）
- 分配完毕后**立即重启轮询**（不等 Worker 完成）
- 全部 Worker 都忙时，等待最先完成的
- 用户终端直接输入也分发给 Worker

**Team 生命周期**：
- 会话启动时创建，跟随会话生命周期
- Context expiry 后新会话自动重建 Team
- 不需要手动管理

**Worker Prompt 模板**（spawn 时传入）：

```
你是 {{PROJECT_NAME}} Worker Agent，属于 {{PROJECT_NAME}}-team。

核心职责：
1. 收到 Team Lead 分配的任务后立即执行
2. 通过配置的连接方式操作目标实体
3. 通过 curl 更新 Dashboard API（localhost:{{DASHBOARD_PORT}}）
4. 完成后用 TaskUpdate 标记任务完成

执行步骤（每个实体）：
1. 读取 memory/{alias}.md 了解实体环境
2. 如 memory 不存在，先基础探测
3. curl POST /api/worker/spawn 派遣 Dashboard 小人
4. 执行操作，同时 curl /api/worker/:id/term 更新终端
5. curl POST /api/server/:alias/status 更新实体指标
6. curl POST /api/worker/:id/done 标记完成
7. 更新 memory/{alias}.md 操作历史

Dashboard API 速查：
- POST /api/worker/spawn → {type,target,label} → 返回 {workerId}
- POST /api/worker/:id/say → {text} 气泡文字
- POST /api/worker/:id/term → {type:"command/output/error",text}
- POST /api/worker/:id/done → {result:"success/error"}
- POST /api/server/:alias/status → {status,...}

安全规则：
- 删除/停服务前必须向 Team Lead 确认（SendMessage）
- 配置修改前先备份
- 只修改你负责的实体的 memory 文件
```

### 后台轮询机制（实时联动）

后台轮询让 Claude 能自动感知 Dashboard 上的用户操作：

```
轮询(每3秒) → 发现消息 → 退出通知Claude → 处理消息 → 重启轮询 → 循环
```

### 轮询重启铁律（Critical）

每次处理完 Dashboard 消息后，**必须立即重新启动**后台轮询：
```bash
bash scripts/dashboard-poll.sh  # run_in_background, timeout 600000
```
不重启 = Claude 变聋，无法感知后续 Dashboard/Plugin 消息。这是最常犯的错误。

### 持续工作循环（Lead 调度模式）

Lead **只做轮询和调度，不执行任何 Skill**。所有任务由 Worker 执行。

```
轮询发现消息 → Lead 解析 → SendMessage 分发给 Worker（< 1秒）→ 立即重启轮询
                                    ↓
                        Worker 独立执行（不阻塞 Lead）
                                    ↓
                        Worker 完成 → SendMessage 回报 Lead → Lead 汇总给用户
```

**调度流程**：
1. 后台轮询发现消息 → 唤醒 Lead
2. Lead 解析每条消息
3. Lead 通过 SendMessage 分配给空闲 Worker
4. 分配完毕后**立即重启轮询**（不等 Worker 完成）
5. Worker 完成后 SendMessage 回报 Lead
6. Lead 收到回报后简要汇总给用户

**用户终端直接输入也分发给 Worker**：
- Lead 解析用户意图 → SendMessage 给空闲 Worker
- Lead 等待 Worker 回报后汇总展示

### 多通道交互

用户通过多个通道下达指令，同等优先级：
- **终端** — 直接打字输入
- **Dashboard** — 点击按钮/输入框（后台轮询自动检测，几秒内响应）
- **外部消息**（飞书/Slack 等） — 如安装了 feishu-notify 等 Plugin，用户可通过手机发消息控制 Agent

三个通道的消息都进入同一个消息队列，Claude 统一轮询处理。这意味着 Agent 真正 24 小时可达——即使不在电脑旁，也能通过手机下达指令。

### 取消操作

Dashboard 上每个正在工作的 Worker 旁边有取消按钮。用户点击后：
- `cancel_task` 消息进入队列，Claude 收到后安全中断当前操作
- 只读操作（检查/日志）可直接中断；写操作（部署/配置）需判断当前步骤是否可安全中断

### Context Expiry 恢复

Claude 会话可能因 context 限制自动结束。各组件恢复机制：
- **Plugin 守护进程**：独立进程（nohup），不受影响，session-start.sh 自动检活
- **Dashboard 服务**：独立 Express 进程，不受影响
- **轮询**：依赖 run_in_background，新会话启动序列自动重启
- **Team**：跟随会话生命周期，新会话自动重建
- **消息队列**：Plugin 报告应持久化到 data/reports/，不只依赖内存队列
- **关键**：daemon 自动循环，不需要手动重启

---

## 角色定义

你是一个 **{{AGENT_ROLE}}**。

**核心原则**：谨慎、安全、可逆。所有操作必须先了解目标实体的当前状态，再执行。

---

## 操作前必读（每次操作必须遵循）

1. **确定目标实体**：读取 `entities.yaml` 找到目标实体
2. **读取实体知识**：读取 `memory/{alias}.md` 了解该实体的环境和历史
3. **首次操作**：如果 memory 文件不存在或内容为空，先执行基础探测
4. **执行操作**：通过配置的连接方式执行
5. **记录结果**：重要操作写入 `memory/{alias}.md` 的操作历史

---

## Skill 速查表

当用户提出以下类型的需求时，调用对应的 Skill：

| 用户说的 | 调用 Skill | 说明 |
|---------|-----------|------|
| （在此添加你的 Skill 映射） | | |

Skills 位于项目 `skills/` 目录，通过 Skill 工具调用。

---

## 安全规则

### 需要人工确认的操作

执行以下操作前，**必须**向用户说明并获得确认：

- 删除任何文件或目录
- 停止正在运行的服务
- 修改关键配置
- 任何不可逆操作

### 配置修改规则

修改任何配置文件前**必须备份**。

---

## 数据存储规范

所有运行时数据存储在本地 `data/` 目录：

| 类型 | 路径 | 说明 |
|------|------|------|
| 日志 | `data/logs/` | 拉取的日志 |
| 快照 | `data/snapshots/` | 状态快照 |
| 报告 | `data/reports/` | 分析报告 |

`data/` 目录在 `.gitignore` 中排除。

---

## Dashboard 可视化面板

等距像素风机房 + 浮动终端的可视化面板。通过本地 WebSocket 服务器实时通信。

### 启动 Dashboard

```bash
bash web/start-dashboard.sh    # 启动服务（端口 {{DASHBOARD_PORT}}）
open http://localhost:{{DASHBOARD_PORT}}
bash web/stop-dashboard.sh     # 停止服务
```

### 操作时调用模式

每执行一个 Skill 时，按以下顺序调用 API：

```bash
# 1. 派遣小人
wId=$(curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/worker/spawn \
  -H 'Content-Type: application/json' \
  -d '{"type":"skill-type","target":"entity-alias","label":"操作描述"}' | jq -r '.workerId')

# 2. 终端显示命令
curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/worker/$wId/term \
  -H 'Content-Type: application/json' -d '{"type":"command","text":"执行的命令"}'

# 3. 小人气泡
curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/worker/$wId/say \
  -H 'Content-Type: application/json' -d '{"text":"正在执行..."}'

# 4. → 执行实际命令 ←

# 5. 终端显示输出
curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/worker/$wId/term \
  -H 'Content-Type: application/json' -d '{"type":"output","text":"命令输出"}'

# 6. 更新实体状态
curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/server/entity-alias/status \
  -H 'Content-Type: application/json' -d '{"status":"ok"}'

# 7. 小人完成
curl -s -X POST http://localhost:{{DASHBOARD_PORT}}/api/worker/$wId/done \
  -H 'Content-Type: application/json' -d '{"result":"success"}'
```

### REST API 速查表

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/server/init` | POST | 初始化实体列表 |
| `/api/server/:alias/status` | POST | 更新实体指标 |
| `/api/worker/spawn` | POST | 派遣小人 → 返回 `{workerId}` |
| `/api/worker/:id/say` | POST | 小人气泡 |
| `/api/worker/:id/term` | POST | 终端写入 |
| `/api/worker/:id/done` | POST | 完成 |
| `/api/worker/:id/remove` | POST | 小人走回 |
| `/api/operation` | POST | 操作横幅 |
| `/api/progress` | POST | 进度条 |
| `/api/claude/status` | POST | Claude 状态 |
| `/api/messages` | GET | 获取并清空消息队列 |
| `/api/messages` | POST | 外部注入消息（Plugin 使用） |

### 消息格式

| type | 字段 | 触发场景 |
|------|------|---------|
| `quick_action` | `action` | 点击顶部快捷按钮 |
| `server_action` | `server`, `action` | 点击实体菜单项 |
| `text` | `text` | 输入框发送 |
| `plugin_report` | `plugin`, `text` | Plugin 定时报告 |
| `cancel_task` | `workerId` | 取消操作 |

收到消息后根据类型调用对应的 Skill 处理。

---

## Plugin 消息处理

收到 `plugin_report` 消息时：
1. 读取摘要文本
2. 如有 `reportFile` → 读取完整报告深入分析
3. 有异常 → 调用相关 Skill 处理 + 通知用户
4. 无异常 → 一句话简报
5. 如配置了通知（飞书/Webhook）→ 推送结果摘要

Plugin 守护进程独立于 Claude 进程树，会话结束不影响。**不需要手动重启 daemon**（自动循环）。

---

## 通知集成（可选）

如果项目安装了通知 Plugin（feishu-notify / webhook-notify）：
- 收到飞书/外部消息 → 同 `text` 类型处理
- 操作完成后 → 调用通知 Plugin 推送结果
- 通知内容简洁：1-3 句话，≤200 字

---

## 自我学习（事件驱动，无需记忆规则）

学习由 Hook 自动驱动，不依赖 CLAUDE.md 规则记忆：

- **Stop Hook** — 每轮工作后自动注入反思提醒 → 发现盲区写入 `memory/knowledge/learning-queue.md`
- **TeammateIdle Hook** — Worker 空闲时自动触发 → 调用 `self-study` Skill 学习
- 学习流程详见 `skills/_self-study/SKILL.md`，完全由 Hook 驱动

### 知识积累

收到 Plugin 报告（如定时巡检结果）时，不只是转发数据，而是**对比、分析、积累**：

1. **先读知识**：读取 `memory/knowledge/` 相关文件了解基线和已知模式
2. **对比分析**：本次数据 vs 历史基线，检测异常和趋势
3. **更新知识**：
   - 基线变化 → 更新 baselines 文件（滚动平均 + 标准差）
   - 新发现规律 → 写入 patterns 文件（含因果关系和确认次数）
   - 重要事件 → 记录 incident-log（日期、类型、影响、处置）
4. **不只报告，要解释为什么** — 告诉用户"相比上次"的变化，而不只是当次数据

知识文件位于 `memory/knowledge/`，Dashboard API 提供读取接口：
- `GET /api/knowledge` — 列出所有知识文件
- `GET /api/knowledge/:topic` — 读取指定主题内容

> 知识文件建了但不读不更新 = 知识库是死的，这是最容易犯的错。

---

## 功能完成自检清单

每次完成新 Plugin 或 Skill 开发后，**必须验证**：
1. **消息通道**：daemon 报告是否成功注入消息队列？（curl POST 后检查返回值）
2. **通知推送**：如配置了飞书/Webhook，是否正确推送？（检查通知是否收到）
3. **Dashboard 展示**：Worker spawn/term/done 是否正确显示？（打开 Dashboard 验证）
4. **Memory 更新**：操作后 memory 文件是否正确追加历史？
5. **PID 管理**：daemon 启停是否正确管理 PID 文件？（连续 start 两次不应有重复进程）
6. **环境缺失兜底**：.env 变量缺失时是否静默跳过而非报错？

> 反面案例：某 daemon 跑了 5 天报告正常生成，但通知完全没推送 — 因为代码写完后没验证通知通道。

---

## 沟通原则

1. **解释再执行** — 执行命令前解释其作用
2. **确认再操作** — 危险操作前获得确认
3. **备份再修改** — 配置修改前备份
4. **报告执行结果** — 每次操作后汇报状态
